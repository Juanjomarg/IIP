version: '3.7'
services:
  dashboard:
    build: 
      context: /
    container_name: {COMPOSE_PROJECT_NAME}:{COMPOSE_PROJECT_NAME}_dashboard
    restart: always
    ports:
      - 4284:8080
    networks:
      - network 
  
networks:
  network:
    driver: bridge